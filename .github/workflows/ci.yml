name: CI

on:
  push:
    branches: 
      - main
  pull_request:
    branches:
        - main

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    if: github.repository != 'EpitechPGE3-2025/G-DEV-500-MPL-5-1-area-3'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        run: npx nx run-many -t test --parallel=3 --exclude=server-e2e

# Ajouter le job e2e-tests si on a des tests e2e à exécuter et qui fonctionne
#   e2e-tests:
#     runs-on: ubuntu-latest
#     needs: unit-tests
#     if: github.repository != 'EpitechPGE3-2025/G-DEV-500-MPL-5-1-area-3'

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm install

#       - name: Run e2e tests
#         run: npx nx run-many -t e2e --parallel=2

  lint:
    runs-on: ubuntu-latest
    if: github.repository != 'EpitechPGE3-2025/G-DEV-500-MPL-5-1-area-3'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run linting
        run: npx nx run-many -t lint --parallel=3

  build:
    runs-on: ubuntu-latest
    if: github.repository != 'EpitechPGE3-2025/G-DEV-500-MPL-5-1-area-3'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: npx nx run-many -t build --parallel=3